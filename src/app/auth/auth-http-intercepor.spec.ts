import { AuthHttpIntercepor } from './auth-http-intercepor';

describe('AuthHttpIntercepor', () => {
  it('should create an instance', () => {
    expect(new AuthHttpIntercepor()).toBeTruthy();
  });
});
